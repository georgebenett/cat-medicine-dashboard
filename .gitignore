# LVGL is cloned by setup.sh (release/v8.3) - not vendored here
lvgl/

# build output
*.o
app
*.log

# device state, written by the app
cat_log.csv
cat_cfg.txt

# written by the timers / hue daemon, regenerated on every run
hue.txt
hue.cmd

# her photos - 6MB of binaries, re-exported whenever the card geometry
# changes, and regenerable from the originals. Copied over with scp.
photos/
cat.png
